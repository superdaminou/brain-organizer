use anyhow::Ok;

use super::panel::PanelTags;



pub fn panel_tags(section: &mut PanelTags, ui: &mut egui::Ui) -> anyhow::Result<()> {
    ui.heading("Tags");

    // créer tags 

    //liste tags
    Ok(())
}